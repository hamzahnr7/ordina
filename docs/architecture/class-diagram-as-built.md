# Class Diagram - As-Built

**Fill this in at the end of the project.** Regenerate from the actual
`app/` code (not from the initial plan), then write 2-3 sentences below on
what changed vs. `docs/planning/class-diagram-initial.md` and why.

Use a visual notation for interface-bound vs. concrete-class dependencies,
e.g. dashed arrow + `<<interface>>` stereotype for the former, solid arrow
for the latter (Mermaid's `..|>` vs `-->` already does this, see the initial
diagram for the convention to keep).

## What changed vs. initial, and why
- TODO
- TODO
