# AI Usage Log

Per brief §6.2 (DISCLOSE / REVIEW / VERIFY / TEST).

| Date | Tool | Purpose | Prompt summary (sanitized) | Output used? | Verification performed |
|------|------|---------|------------------------------|----------------|--------------------------|
| 2026-09-03 | Claude Code | Scaffold initial project structure (Docker, folder layout, layered-architecture skeleton, DB schema draft, docs templates) from the uploaded Project Brief PDF | "Init project per brief, generate template structure; use Redis to make dev easier" | Yes - structure kept, to be filled in with real business logic by hand | Manually reviewed folder layout and docker-compose against brief §4.1/§5.1; schema checked against §1.3 entity table; no code has been run yet - functional verification pending first `docker compose up` |
| 2026-09-10 | Claude Code | Implement login/session (AUTH-01), Admin-only user management (USR-01), and a Role/Permission/Gate/MenuRegistry authorization layer; document the RBAC architecture and how to extend it | "Buatkan fitur login dan session management, user management, dan arsitektur role/akses menu beserta cara menambah role/menu baru" | Yes - code + `docs/architecture/rbac-and-menu-access.md` + `adr-0004` kept, unit tests added (`AuthServiceTest`, `UserServiceTest`, `GateTest`) | Manually traced each `Gate::allows()` rule against brief §1.2's SoD table; confirmed `Controller::authorize()` throws (not renders) so ERR-01's centralized handling in `public/index.php` covers it; **not yet run inside Docker/PHPUnit** at authoring time - PHP CLI unavailable in this authoring environment. Participant later ran `docker compose exec web composer test` themselves and iterated with this tool to fix real environment issues found along the way (wrong `DB_TEST_HOST`, missing `tester` MySQL user/`ordina_test` DB, `depends_on` not waiting for MySQL's healthcheck) until all tests passed |
| 2026-09-10 | Claude Code | Implement master data: Category/Warehouse/Supplier/Customer CRUD and Product CRUD with search/filter/pagination (FIND-01), image upload (PRD-01), and per-warehouse stock detail view (WH-01) | "Lanjutkan proses developmentnya" (continue development, following the brief's build order after unit tests passed) | Yes - code + `ProductServiceTest`/`InMemoryCategoryRepository` kept; docs updated (`tech-debt.md`, `test-scenarios.md`, `class-diagram-as-built.md`, `user-stories.md`, README) | Manually re-derived the `paginateForListing()` SQL and caught/fixed a real bug before it shipped: the `HAVING total_stock < p.reorder_point` clause referenced an alias not present in the `COUNT(*)` subquery's SELECT list, which would have thrown "Unknown column" in MySQL. **Not run against real MySQL/PHPUnit** at authoring time (no Docker daemon available here) - participant should run `composer test` and click through `/categories`, `/warehouses`, `/suppliers`, `/customers`, `/products` (including an image upload) before trusting this slice |
| 2026-09-11 | Claude Code | Fixed two production bugs the participant hit after deploying the master-data slice (a 500 on `/products`): first misdiagnosed as MySQL `ONLY_FULL_GROUP_BY`/repeated-placeholder issues (both real, both fixed) before the actual root cause - a call to a `ProductService::normalizeFilters()` method that was never implemented - surfaced via the participant's PHP error log | "coba cek menu produk karena halaman tersebut menampilkan code 500", then pasted `docker compose logs` output on request | Yes - `MysqlProductRepository::paginateForListing()` SQL rewritten (subquery-based, no GROUP BY/HAVING), phantom `normalizeFilters()` call removed | Confirmed via the participant's own `docker compose logs web` output (exact exception + stack trace) after each fix attempt - this is the one entry in this log backed by a real runtime error message, not just static code review |
| 2026-09-11 | Claude Code | Added a "/" redirect (session -> /dashboard, none -> /login) via new `HomeController`; implemented Purchase Order + goods receipt (PO-01): full CRUD/status lifecycle (Draft/Ordered/PartiallyReceived/Received/Cancelled), FIND-01 search/filter/sort/pagination on the PO list, and a `TransactionManagerInterface` seam so goods receipt's ProductStock+StockLedger write is both a real MySQL transaction in production and unit-testable with fakes | "tolong buatkan redirect login..." then "lanjut ke Purchase Order" | Yes - all code kept; `PurchaseOrderServiceTest` (9 cases) + `InMemorySupplierRepository`/`InMemoryWarehouseRepository` added; `adr-0002-concurrency-safe-stock.md` updated to record what's actually built vs. still TODO for SO-01 | Manually traced the goods-receipt transaction boundary and the status-recompute logic (Draft/Ordered/PartiallyReceived/Received) against brief PO-01's ketentuan minimum; reasoned explicitly about why PO's receipt needs atomicity only, not the pessimistic-lock mechanism ARCH-02 asks for (that's reserved for SO's goods issue). **Not run against real MySQL/PHPUnit or clicked through in a browser** at authoring time (no Docker daemon available here) - participant should run `composer test` and manually exercise create -> mark ordered -> partial receipt -> full receipt before trusting this slice |
| 2026-09-11 | Claude Code | Participant ran `composer test` and hit one failing PO test; fixed it | "sudah pass semua tests" (after) / earlier turn pasted the PHPUnit failure output | Fixed `tests/Unit/PurchaseOrderServiceTest.php::test_cannot_receive_more_than_remaining_quantity` - it expected `ValidationException` but a full-qty first receipt actually moves the PO to `Received`, so the second call is correctly rejected by the status guard (`ForbiddenOperationException`) instead; the *production code* was right, the test's expectation was wrong | Confirmed by re-reading `PurchaseOrderService::receiveGoods()`'s guard order against the participant's exact PHPUnit failure message (expected/actual exception class + line numbers); added a second, distinct test for the status-guard case instead of just patching the assertion |
| 2026-09-11 | Claude Code | Implemented Sales Order + approval + goods issue (SO-01), and finalized/implemented ARCH-02's concurrency mechanism (previously only planned for PO): full CRUD/status lifecycle (Draft/PendingApproval/Approved/Fulfilled/Cancelled), FIND-01 on the SO list, server-side self-approval block (creator can never approve their own order, any role), and `ProductStockRepositoryInterface::decrementIfAvailable()` - an atomic conditional `UPDATE` chosen over `SELECT ... FOR UPDATE` for goods issue's oversell prevention | "lanjutkan ke point 9" (continuing a numbered feature list from an earlier turn: Sales Order + approval + goods issue) | Yes - all code kept; `SalesOrderServiceTest` (12 cases, including the core oversell-rejection scenario) + `InMemorySalesOrderRepository`/`InMemorySalesOrderItemRepository`/`InMemoryCustomerRepository` added; `adr-0002-concurrency-safe-stock.md` updated from "TODO" to a fully reasoned Decision section | Caught and fixed a real bug in a *view* file before it shipped: `views/sales-orders/index.php` initially called `$this->currentRole()` - views are plain includes run from a static method (`App\Core\View::render()`), so `$this` isn't available there and this would have fataled; fixed by passing a `canCreate` boolean from the controller instead, matching the pattern already used for Product/PO's `canManage`. **Not run against real MySQL/PHPUnit or clicked through in a browser** at authoring time (no Docker daemon available here) - participant should run `composer test` and manually exercise create -> submit -> approve (as a different user) -> goods issue, plus the self-approval rejection and the oversell scenario, before trusting this slice |

| 2026-09-11 | Claude Code | Answered a question (who approves a submitted Sales Order) with no code change, then implemented the role-based dashboard (DASH-01): a new `DashboardRepositoryInterface`/`MysqlDashboardRepository` for all aggregation queries (inventory value, low-stock count/list, PO/SO status breakdowns, pending goods-receipt/issue counts), `DashboardService` (one method per role), and the dashboard view rendering role-specific stat tiles | "oke, lanjutkan ke fitur selanjutnya" (continuing the numbered feature list - DASH-01 was next after SO-01) | Yes - all code kept; docs updated (`tech-debt.md`, `test-scenarios.md` n/a here since no new unit tests, `user-stories.md`, README) | Caught and fixed my own leftover mistake before it shipped: `MysqlDashboardRepository::lowStockProducts()` had a dead, syntactically-nonsensical first `prepare()` call (scaffolding debris from drafting the query) immediately overwritten by the real one - removed it. Deliberately did **not** add an InMemory fake/unit test for this repository (pure aggregation SQL, no business logic to verify without a database - consistent with Category/Warehouse/Supplier/Customer's repositories) - tracked as tech-debt #17 instead. **Not run against real MySQL or viewed in a browser** at authoring time (no Docker daemon available here) - participant should run `docker compose up`, log in as each of the three roles, and confirm the dashboard numbers match what's actually in the seed data before trusting this slice |

| 2026-09-11 | Claude Code | Implemented CSV reports (REPORT-01) - the last WAJIB functional slice from brief §2: date-ranged stock-ledger export and combined PO+SO status export, access mirroring §1.2's download-report row (Admin = both/all, Sales = orders/own only, Warehouse Staff = stock only) | "lanjutkan" (continuing after CSV reports were named as the next item) | Yes - all code kept; `findForReport()`/`findByDateRange()` added to the existing PO/SO/StockLedger repositories (reused by `ReportService`, not a new data path) + matching `InMemory` fake updates so existing PO/SO tests keep passing | Verified the existing `PurchaseOrderServiceTest`/`SalesOrderServiceTest` suites still type-check against the now-larger repository interfaces by updating every `InMemory` fake's method set to match. **No new automated test written** for `ReportService`/`ReportController` (tracked as tech-debt #18 - reporting/query composition, same reasoning as DASH-01's repository) and **not run against real MySQL or clicked through in a browser** at authoring time (no Docker daemon available here) - participant should download both CSVs as each role and confirm row counts/content against known seed data, including two different date ranges per the brief's explicit bukti requirement |

| 2026-09-14 | Claude Code (`frontend-design` skill) | Restructured the app shell from a top nav bar to a grouped sidebar (Dashboard / Master Data / Transaksi / Laporan / Administrasi sections, one inline-SVG icon per item), with an off-canvas drawer + overlay on mobile | "tolong perbaiki design tampilan websitenya menjadi sidebar, menu yang ada pada sidebar bisa dikelompokkan agar menjadi lebih rapih" | Yes - `config/menus.php` (group/icon metadata), `MenuRegistry::forRole()` (now returns grouped sections), `views/layouts/app.php`, `public/assets/css/app.css`, `public/assets/js/app.js` all kept | Presentational-only change - confirmed by grep that `.app-header`/`.app-nav`/`.app-user` (the replaced classes) were referenced only in the three files edited, nothing else in the codebase. **Not viewed in a browser** at authoring time (no Docker daemon available here) - participant should click through as each of the three roles and check the mobile drawer at ~360px width before trusting this slice |
| 2026-09-14 | Claude Code (`frontend-design` skill) | Modernized every create/edit form (shared `.form-card`/`.form-grid`/`.form-actions` CSS system applied across all 8 entities, required-field markers, Batal/Simpan footer) and every FIND-01 search/filter bar (search-icon input, a "Reset filter" link shown only when a filter is active); restyled the deactivate ("Nonaktifkan") action with a danger tint plus a confirm-before-submit prompt (soft-delete only, never a hard delete) | "tolong perbaiki design setiap halaman seperti halaman create, update, dan delete, lalu perbaiki juga halaman yang berisi pencarian agar terlihat lebih modern" | Yes - all 16 create/edit views, 3 filter-bar index views, 5 toggle-active index views, `app.css`, `app.js` kept | Visual/markup-only change, no controller/service/repository logic touched - reused the exact `$filters`/`$old`/entity variable names each controller already passes (verified per file while editing, e.g. `$product->categoryId`, `$filters['status']`). **Not viewed in a browser** at authoring time (no Docker daemon available here) - participant should click through each create/edit form (including validation-error re-display) and the deactivate confirm dialog before trusting this slice |

| 2026-09-15 | Claude Code (`frontend-design` skill) | Restructured page-level layout (not just visual polish) across the whole app: a shared `.page-head` component (title/meta/action) replacing the old back-link+toolbar pair on every index/create/edit/show page; PO/SO/Product detail pages rebuilt as a two-column layout (item table + history in the main column, a persistent summary/actions sidebar - `.detail-layout`); the dashboard rebuilt as a grid with PO/SO status shown as proportional bars (`.status-bar-*`) instead of flat badge rows, plus a dedicated low-stock panel; the login page rebuilt as a split-screen (form + a CSS-only warehouse-grid visual panel); removed the now-redundant "back to dashboard" link from every index page since the sidebar already provides that | "desain ulang setiap halaman agar lebih modern tidak hanya efek saja tapi dari tata letak komponennya juga" | Yes - all edited views, `app.css` (added `.page-head`, `.detail-layout`, `.dashboard-grid`/`.panel`/`.status-bar-*`, `.auth-visual` split-screen; removed the now-dead `.back-link`/`.toolbar`/`.status-counts` rules after confirming via grep no view referenced them anymore) kept | Grep-verified before deleting any CSS rule that no view still used the class being removed. Layout-only change, no controller/service/repository logic touched. **Not viewed in a browser** at authoring time (no Docker daemon available here) - participant should check the PO/SO/Product detail pages (sidebar actions staying reachable while scrolling a long item list), the dashboard status bars against real seed data, and the login page at both desktop and mobile widths before trusting this slice |

| 2026-09-15 | Claude Code (`frontend-design` skill) | Removed the "Kembali ke daftar X" link from every create/edit form's header (14 files) - the header now shows just the title plus a one-line, entity-specific subtitle instead; the existing "Batal" button in each form's footer (already linking back to the same list) is now the only back action, so there's no redundant pair of "leave this form" affordances | "coba ubah tampilan create form pada menu manapun agar lebih modern dan hapus juga tombol kembali di bagian header halaman tersebut ... ganti dengan tombol yang lebih bagus ... atau bisa juga menggunakan tombol batal pada form" | Yes - all 14 create/edit views kept. Left the same back link (`.page-back`) in place on the 3 detail/show pages (PO/SO/product), since those don't have a form-Batal equivalent and weren't what this request was about | Grep-confirmed `.page-back` now appears only on the 3 show pages it was intentionally kept on. Text-only/markup change, no logic touched. **Not viewed in a browser** at authoring time (no Docker daemon available here) - participant should confirm the header reads cleanly without the link and that Batal still returns to the right list on each of the 14 forms |

| 2026-09-15 | Claude Code (`frontend-design` skill) | Participant shared a screenshot of `/purchase-orders/create` and asked whether it looked modern; critiqued it (dead space beside the form card, a plain item table with no currency affordance, heavy "Hapus" text buttons) and, per their choice, fixed all three on both PO and SO create forms: two-column `.detail-layout` (form + a live "Ringkasan Order" sidebar panel), a `Rp`-prefixed price input and a per-row Subtotal column, and an icon-only remove button | "apakah tampilan tersebut terlihat modern?" (screenshot) -> picked all 3 suggested fixes | Yes - `views/purchase-orders/create.php`, `views/sales-orders/create.php`, `app.css` (`.icon-btn`, `.input-prefix`, `.row-subtotal`), `app.js` (new live-recalculation IIFE: per-row subtotal + item count/total qty/estimated total, driven by native `input` events plus a `MutationObserver` so it also re-runs after the existing add/remove-row handlers mutate the table) kept | The summary panel and per-row subtotal are a **progressive-enhancement display only** - if JS fails to load they stay at their static "0"/"Rp0" markup, which doesn't affect what's actually submitted (qty/price fields post their real values regardless) - confirmed by re-reading `PurchaseOrderService`/`SalesOrderService` still only read `items[...][]` POST fields, untouched by this change. **Not viewed in a browser** at authoring time (no Docker daemon available here) - participant should type into qty/price fields on both forms and confirm the subtotal per row and the sidebar total update live, including after add/remove item |

| 2026-09-15 | Claude Code (`frontend-design` skill) | Participant shared a screenshot of `/products/create` and asked for a more modern look; applied the same `.detail-layout` two-column treatment as PO/SO (fixing the dead space beside the card), added a live image preview panel (Vanilla JS `FileReader`, no library) since a product photo is the one thing on this form you can't otherwise verify before saving, restyled the raw browser file-input chrome (`::file-selector-button`) to match the rest of the form, and added the same `Rp` prefix used on PO/SO price fields to Harga Beli/Harga Jual for consistency | Screenshot + "perbaiki halaman ini agar halaman tersebut terlihat lebih modern" | Yes - `views/products/create.php`, `views/products/edit.php`, `app.css` (`.image-preview`, `input[type=file]::file-selector-button`), `app.js` (new preview IIFE) kept. Edit's old inline "Gambar saat ini" block replaced by the same sidebar panel, pre-filled with the product's current image | The preview is progressive enhancement only (upload still works from `input[type=file]` regardless of JS); confirmed the `<input name="image">` itself is unchanged, so `ProductImageUploader` still receives the same `$_FILES` shape. **Not viewed in a browser** at authoring time (no Docker daemon available here) - participant should pick a file on both create and edit and confirm the preview updates, and that edit's existing image still shows before a new one is chosen |

| 2026-09-15 | Claude Code | On Purchase Order create, Harga Beli now auto-fills from the selected product's own registered buy price (`data-buy-price` on each `<option>`, read via a `change` listener on `.row-product`) - still a plain editable input afterwards, just a starting point instead of blank. Also caught and fixed the earlier alignment fix (`style="margin-bottom:0;"` on the product select) not actually reaching the "add item" `<template>` row - only the first row had it | "pada purchase order create tambahkan harga beli yang tercantum pada data produk" | Yes - `views/purchase-orders/create.php`, `app.js` (new auto-fill IIFE) kept | The auto-fill dispatches a synthetic `input` event on the price field so the existing live-subtotal calculator picks up the change automatically, without coupling the two IIFEs together. Confirmed `Product` entity already exposes public `buyPrice` (used elsewhere in `products/edit.php`), so no repository/service change was needed - purely a view+JS change. Deliberately scoped to PO only (Harga Beli) - Sales Order's Harga Jual auto-fill was not requested and not built. **Not viewed in a browser** at authoring time (no Docker daemon available here) - participant should confirm picking a product fills Harga Beli and updates the row's Subtotal, on both the first row and a row added via "+ Tambah Item" |

| 2026-09-15 | Claude Code | Added the same product-price auto-fill to Sales Order create (Harga Jual from `data-sell-price`), generalizing the PO auto-fill JS into one handler for both. Per the participant's follow-up, PO's Harga Beli field is now `disabled` after auto-fill (always mirrors the product's registered buy price, no manual override) - implemented as a disabled visible field plus a hidden twin input (`items[buy_price][]`) kept in sync by JS, since a `disabled` field's value is never submitted with the form. Also fixed the same "add item" template-row alignment bug on SO that was found and fixed on PO last time | "lakukan hal tersebut pada sales order create dan tampilkan harga jual dari produk tersebut, untuk purchase order mungkin bisa ditambahkan disable edit" | Yes - `views/sales-orders/create.php`, `views/purchase-orders/create.php`, `app.js` (auto-fill handler generalized) kept | Confirmed `disabled` inputs are excluded from form submission entirely, which is why the hidden `.row-price-value` mirror exists - PurchaseOrderService still receives `items[buy_price][]` exactly as before, so no server-side change was needed; the live-subtotal calculator still reads the (disabled but JS-readable) visible field, unaffected. **Not viewed in a browser** at authoring time (no Docker daemon available here) - participant should confirm PO's Harga Beli field visually looks disabled and still submits a correct value on save, and that SO's Harga Jual auto-fills but stays editable |

| 2026-09-15 | Claude Code | Added a "Stok Tersedia" column to Sales Order create's item table, showing each selected product's stock at the selected Gudang Asal - finally wiring up the `fetchJson()` helper and API-01 endpoint (`/api/products/{sku}/availability`) that had sat defined-but-unused in `app.js` since the initial JS scaffolding. Turns red (badge-danger) if the entered qty exceeds that stock, informational only | "tambahkan stock produk pada menu sales order create" | Yes - `views/sales-orders/create.php` (new column + `data-sku` on product options + updated field-hint wording), `app.js` (new stock-lookup IIFE, per-SKU cached via a `Map` to avoid refetching on every keystroke) kept | Re-read `ProductAvailabilityService::availabilityBySku()` to confirm the exact response shape (`{sku, name, warehouses:[{warehouse_id, warehouse_name, quantity}]}`) before matching on `warehouse_id`. Deliberately informational, not a submission gate - reconfirmed with the participant's own already-existing field-hint that the real oversell check (ARCH-02) only happens at goods issue; updated that hint's wording so it now explicitly says the displayed stock is reference-only. **Not viewed in a browser** at authoring time (no Docker daemon available here) - participant should pick a product + warehouse and confirm the stock badge loads (network tab should show one `/api/products/.../availability` call per distinct SKU, not per keystroke) |

| 2026-09-16 | Claude Code | Fixed a real bug the participant screenshotted on `/login`: a vertical scrollbar and the dark visual panel not reaching the left edge of the viewport. Root cause - the logged-out branch of `views/layouts/app.php` wrapped every page's content in `.container` (`max-width:1200px; margin:0 auto; padding: var(--space-4)`), which is meant for regular padded/centered pages; stacked on top of the login page's own `min-height:100vh` split-screen, that extra padding pushed total height past 100vh (the scrollbar) and centered/inset the whole edge-to-edge layout instead of letting it touch the viewport edges | Screenshot + "tolong buat halaman tersebut tidak memiliki overflow dan juga bagian sebelah kiri yang ada text itu terpotong harusnya full sampai ke ujung kiri halaman" | Yes - `AuthController::showLogin()` now passes `'bleed' => true`, and the layout's logged-out branch renders `$content` directly (no `.container`) when that flag is set, otherwise unchanged - kept | Traced the exact CSS math (100vh + 2x var(--space-4) padding = overflow) before touching anything, rather than guessing at a CSS-only patch. Scoped narrowly - only the login page passes `bleed`, so error pages (403/404/500) and every other anonymous-context render keep their existing `.container` wrap untouched. **Not viewed in a browser** at authoring time (no Docker daemon available here) - participant should confirm `/login` now has no scrollbar and the dark panel is flush to the left edge at both desktop and the ~760px breakpoint where the panel hides |

| 2026-09-16 | Claude Code | Applied the same `.detail-layout` two-column fix used on PO/SO/Product create to `/reports` - same "card capped at 620px, rest of a 1200px container left empty" complaint. The date-range form now fills the main column; the explanatory text that used to sit as a hint below the fields (what Laporan Stok/Laporan Order each contain) moved into a "Cakupan Laporan" sidebar panel instead of empty space | Screenshot + "pada halaman laporan lebar isi menu belum maksimal tolong perbaiki" | Yes - `views/reports/index.php` kept, no CSS/JS changes needed (reused existing `.detail-layout`/`.panel` components) | Purely reused the established layout system rather than inventing a new one-off pattern for this page - fourth page now using the same detail-layout shape (PO/SO/Product create, now Reports), keeping the app visually consistent. **Not viewed in a browser** at authoring time (no Docker daemon available here) - participant should confirm the date fields + buttons still work and the sidebar panel reads correctly on both desktop and mobile |

| 2026-09-18 | Claude Code | New business rule: a Sales Order's Harga Jual may only be raised from the product's own registered sell price, never lowered. Enforced server-side in `SalesOrderService::validateItems()` (rejects `sell_price < product->sellPrice`, equal/above both pass) - the actual authority, since the client can't be trusted alone; mirrored client-side by setting the price input's `min` attribute to the product's price when auto-filled, for instant feedback before submit | "tambahkan fitur agar pada saat sales order itu tidak bisa mengurangi harga penjualan barang, bisanya hanya menaikkan harga tersebut" | Yes - `app/Service/SalesOrderService.php`, `views/sales-orders/create.php` (new hint line), `public/assets/js/app.js` kept; added `tests/Unit/SalesOrderServiceTest.php::test_create_rejects_sell_price_below_product_price` and `test_create_accepts_sell_price_at_or_above_product_price`, plus a row in `docs/testing/test-scenarios.md` | Checked the existing tests' fixture data (`sellPrice: 2000`, all existing `create()` calls submit exactly `2000`) before changing the comparison to strict `<`, confirming equal-to-product-price still passes and nothing already-passing breaks. **Not run against PHPUnit or viewed in a browser** at authoring time (no PHP/Docker available here) - participant should run `composer test:unit` and manually try creating an SO with a price below vs. above the product's price |

| 2026-09-18 | Claude Code | Added the official long-form name "Order Management & Inventory System" (provided by the participant) alongside the "Ordina" wordmark: a subtitle line on the login hero panel, the browser tab's default `<title>`, and README's H1, so the full name is consistent everywhere the short brand name already appears | "tolong tambahkan title kepanjangan dari ORDINA" -> "Order Management & Inventory System" | Yes - `views/auth/login.php`, `views/layouts/app.php`, `README.md`, `app.css` (new `.auth-visual-subtitle`) kept | Text/copy change only, no logic touched - nothing to verify beyond reading the new text in a browser |

| 2026-09-18 | Claude Code | Added a "Tampilkan" (rows-per-page) selector to Produk/Purchase Order/Sales Order list tables - default 10, selectable 25/50, auto-submits on change - plus a "Menampilkan X-Y dari Z data" line next to the pagination controls (which per the earlier request now always render, even on a single page) | "tambahkan jumlah rows pada tabel ... buat default 10 dan bisa diubah manjadi 25 dan 50" | Yes - `ProductService`/`PurchaseOrderService`/`SalesOrderService::paginate()` (renamed `PER_PAGE` to `DEFAULT_PER_PAGE`, added an overridable `$perPage` param, default unchanged at 10 so no existing caller breaks), the 3 index controllers (parse+validate `per_page` against `[10,25,50]`, default 10), the 3 index views (`per_page` select + range text + `per_page` folded into `$queryWithout()` so page-number links keep the chosen page size), `app.js` (generic `#per_page` change -> `form.submit()`), `app.css` (`.pagination-bar`, `.pagination-info`, `.per-page-field`) kept | Grepped for every other caller of `paginate()` before changing its signature - none exist outside the 3 controllers, and the new parameter is optional with the same default, so nothing else could break. **Not run against PHPUnit or viewed in a browser** at authoring time (no PHP/Docker available here) - participant should confirm changing "Tampilkan" reloads the list at the new size and that page-number links preserve it |

| 2026-09-18 | Claude Code | Fixed a real 500 error (screenshotted by the participant) on `/purchase-orders` - and the same latent bug on `/products` and `/sales-orders`, all introduced by the previous per-page-selector change: `in_array((int) ($_GET['per_page'] ?? 10), [10,25,50], true) ? (int) $_GET['per_page'] : 10` re-reads `$_GET['per_page']` directly (no `??`) in the ternary's true branch - since `10` is itself a valid value, visiting the page with no `per_page` in the URL at all made the check pass and then hit the un-coalesced re-read, an undefined-array-key warning this app's error handler escalates to a fatal 500 | Screenshot of the 500 + "cek juga pada fitur products" | Yes - `ProductController.php`, `PurchaseOrderController.php`, `SalesOrderController.php` (all three had the identical bug) kept - now compute `$requestedPerPage` once and validate that, never re-touching `$_GET` | Diagnosed directly from the warning message in the screenshot (exact file+line), then grepped for the same `in_array(...$_GET...) ? ...$_GET... :` pattern across the other two controllers proactively rather than waiting for the participant to hit each one separately - found and fixed all three in one pass. **Not run against PHPUnit or viewed in a browser** at authoring time (no PHP/Docker available here) - participant should reload `/purchase-orders`, `/products`, and `/sales-orders` with no query string at all to confirm the 500 is gone |

| 2026-09-18 | Claude Code | Restyled the small muted "Kembali ke daftar X" text link on PO/SO/Product detail pages (the participant found it visually weak, floating on its own line above the title) into a compact circular icon-only back button placed inline right beside the page title - reuses the existing `.icon-btn` component already used elsewhere (the item-remove trash button) rather than inventing a new one | Two screenshots (PO-00005, SO-00011 detail pages) + "perbaiki tampilan tombol back tersebut dan pindahkan ke bagian yang lebih bagus agar ui/ux lebih bagus lagi" | Yes - `views/purchase-orders/show.php`, `views/sales-orders/show.php`, `views/products/show.php` (fixed for consistency even though only PO/SO were named - all three shared the identical old pattern), `app.css` (`.page-title-row`, `.back-btn`; removed the now-dead `.page-back` rule after confirming via grep no view still used it) kept | Caught and fixed a real latent bug while building this: `.icon-btn` only got `display:inline-flex`/`align-items:center` from the separate `button, .btn` base rule, which the new back button (an `<a>`, not a `<button>`, and without the `.btn` class) would never have matched - the icon would've rendered as an unstyled inline `<a>` with width/height silently ignored. Made `.icon-btn` fully self-contained (own `display`/`align-items`/`text-decoration`/`cursor`) instead of relying on that co-occurrence, which also make it safe to use as a link elsewhere in the future. **Not viewed in a browser** at authoring time (no Docker daemon available here) - participant should confirm the circular back button renders correctly (not as a stretched/unstyled link) and that its click target still navigates to the right list |

## Notes
- All architecture decisions in `docs/architecture/adr-*.md` must be
  re-explained by the participant unaided during technical defense,
  regardless of how they were drafted.
- Update this table every time an AI tool materially contributes code,
  docs, or design decisions - including "no AI used this session" entries
  if applicable, per the brief's requirement to disclose even non-use.
