# AI Usage Log

Per brief §6.2 (DISCLOSE / REVIEW / VERIFY / TEST).

| Date | Tool | Purpose | Prompt summary (sanitized) | Output used? | Verification performed |
|------|------|---------|------------------------------|----------------|--------------------------|
| 2026-09-03 | Claude Code | Scaffold initial project structure (Docker, folder layout, layered-architecture skeleton, DB schema draft, docs templates) from the uploaded Project Brief PDF | "Init project per brief, generate template structure; use Redis to make dev easier" | Yes - structure kept, to be filled in with real business logic by hand | Manually reviewed folder layout and docker-compose against brief §4.1/§5.1; schema checked against §1.3 entity table; no code has been run yet - functional verification pending first `docker compose up` |
| 2026-09-10 | Claude Code | Implement login/session (AUTH-01), Admin-only user management (USR-01), and a Role/Permission/Gate/MenuRegistry authorization layer; document the RBAC architecture and how to extend it | "Buatkan fitur login dan session management, user management, dan arsitektur role/akses menu beserta cara menambah role/menu baru" | Yes - code + `docs/architecture/rbac-and-menu-access.md` + `adr-0004` kept, unit tests added (`AuthServiceTest`, `UserServiceTest`, `GateTest`) | Manually traced each `Gate::allows()` rule against brief §1.2's SoD table; confirmed `Controller::authorize()` throws (not renders) so ERR-01's centralized handling in `public/index.php` covers it; **not yet run inside Docker/PHPUnit** at authoring time - PHP CLI unavailable in this authoring environment. Participant later ran `docker compose exec web composer test` themselves and iterated with this tool to fix real environment issues found along the way (wrong `DB_TEST_HOST`, missing `tester` MySQL user/`ordina_test` DB, `depends_on` not waiting for MySQL's healthcheck) until all tests passed |
| 2026-09-10 | Claude Code | Implement master data: Category/Warehouse/Supplier/Customer CRUD and Product CRUD with search/filter/pagination (FIND-01), image upload (PRD-01), and per-warehouse stock detail view (WH-01) | "Lanjutkan proses developmentnya" (continue development, following the brief's build order after unit tests passed) | Yes - code + `ProductServiceTest`/`InMemoryCategoryRepository` kept; docs updated (`tech-debt.md`, `test-scenarios.md`, `class-diagram-as-built.md`, `user-stories.md`, README) | Manually re-derived the `paginateForListing()` SQL and caught/fixed a real bug before it shipped: the `HAVING total_stock < p.reorder_point` clause referenced an alias not present in the `COUNT(*)` subquery's SELECT list, which would have thrown "Unknown column" in MySQL. **Not run against real MySQL/PHPUnit** at authoring time (no Docker daemon available here) - participant should run `composer test` and click through `/categories`, `/warehouses`, `/suppliers`, `/customers`, `/products` (including an image upload) before trusting this slice |
| 2026-09-11 | Claude Code | Fixed two production bugs the participant hit after deploying the master-data slice (a 500 on `/products`): first misdiagnosed as MySQL `ONLY_FULL_GROUP_BY`/repeated-placeholder issues (both real, both fixed) before the actual root cause - a call to a `ProductService::normalizeFilters()` method that was never implemented - surfaced via the participant's PHP error log | "coba cek menu produk karena halaman tersebut menampilkan code 500", then pasted `docker compose logs` output on request | Yes - `MysqlProductRepository::paginateForListing()` SQL rewritten (subquery-based, no GROUP BY/HAVING), phantom `normalizeFilters()` call removed | Confirmed via the participant's own `docker compose logs web` output (exact exception + stack trace) after each fix attempt - this is the one entry in this log backed by a real runtime error message, not just static code review |
| 2026-09-11 | Claude Code | Added a "/" redirect (session -> /dashboard, none -> /login) via new `HomeController`; implemented Purchase Order + goods receipt (PO-01): full CRUD/status lifecycle (Draft/Ordered/PartiallyReceived/Received/Cancelled), FIND-01 search/filter/sort/pagination on the PO list, and a `TransactionManagerInterface` seam so goods receipt's ProductStock+StockLedger write is both a real MySQL transaction in production and unit-testable with fakes | "tolong buatkan redirect login..." then "lanjut ke Purchase Order" | Yes - all code kept; `PurchaseOrderServiceTest` (9 cases) + `InMemorySupplierRepository`/`InMemoryWarehouseRepository` added; `adr-0002-concurrency-safe-stock.md` updated to record what's actually built vs. still TODO for SO-01 | Manually traced the goods-receipt transaction boundary and the status-recompute logic (Draft/Ordered/PartiallyReceived/Received) against brief PO-01's ketentuan minimum; reasoned explicitly about why PO's receipt needs atomicity only, not the pessimistic-lock mechanism ARCH-02 asks for (that's reserved for SO's goods issue). **Not run against real MySQL/PHPUnit or clicked through in a browser** at authoring time (no Docker daemon available here) - participant should run `composer test` and manually exercise create -> mark ordered -> partial receipt -> full receipt before trusting this slice |
| 2026-09-11 | Claude Code | Participant ran `composer test` and hit one failing PO test; fixed it | "sudah pass semua tests" (after) / earlier turn pasted the PHPUnit failure output | Fixed `tests/Unit/PurchaseOrderServiceTest.php::test_cannot_receive_more_than_remaining_quantity` - it expected `ValidationException` but a full-qty first receipt actually moves the PO to `Received`, so the second call is correctly rejected by the status guard (`ForbiddenOperationException`) instead; the *production code* was right, the test's expectation was wrong | Confirmed by re-reading `PurchaseOrderService::receiveGoods()`'s guard order against the participant's exact PHPUnit failure message (expected/actual exception class + line numbers); added a second, distinct test for the status-guard case instead of just patching the assertion |
| 2026-09-11 | Claude Code | Implemented Sales Order + approval + goods issue (SO-01), and finalized/implemented ARCH-02's concurrency mechanism (previously only planned for PO): full CRUD/status lifecycle (Draft/PendingApproval/Approved/Fulfilled/Cancelled), FIND-01 on the SO list, server-side self-approval block (creator can never approve their own order, any role), and `ProductStockRepositoryInterface::decrementIfAvailable()` - an atomic conditional `UPDATE` chosen over `SELECT ... FOR UPDATE` for goods issue's oversell prevention | "lanjutkan ke point 9" (continuing a numbered feature list from an earlier turn: Sales Order + approval + goods issue) | Yes - all code kept; `SalesOrderServiceTest` (12 cases, including the core oversell-rejection scenario) + `InMemorySalesOrderRepository`/`InMemorySalesOrderItemRepository`/`InMemoryCustomerRepository` added; `adr-0002-concurrency-safe-stock.md` updated from "TODO" to a fully reasoned Decision section | Caught and fixed a real bug in a *view* file before it shipped: `views/sales-orders/index.php` initially called `$this->currentRole()` - views are plain includes run from a static method (`App\Core\View::render()`), so `$this` isn't available there and this would have fataled; fixed by passing a `canCreate` boolean from the controller instead, matching the pattern already used for Product/PO's `canManage`. **Not run against real MySQL/PHPUnit or clicked through in a browser** at authoring time (no Docker daemon available here) - participant should run `composer test` and manually exercise create -> submit -> approve (as a different user) -> goods issue, plus the self-approval rejection and the oversell scenario, before trusting this slice |

| 2026-09-11 | Claude Code | Answered a question (who approves a submitted Sales Order) with no code change, then implemented the role-based dashboard (DASH-01): a new `DashboardRepositoryInterface`/`MysqlDashboardRepository` for all aggregation queries (inventory value, low-stock count/list, PO/SO status breakdowns, pending goods-receipt/issue counts), `DashboardService` (one method per role), and the dashboard view rendering role-specific stat tiles | "oke, lanjutkan ke fitur selanjutnya" (continuing the numbered feature list - DASH-01 was next after SO-01) | Yes - all code kept; docs updated (`tech-debt.md`, `test-scenarios.md` n/a here since no new unit tests, `user-stories.md`, README) | Caught and fixed my own leftover mistake before it shipped: `MysqlDashboardRepository::lowStockProducts()` had a dead, syntactically-nonsensical first `prepare()` call (scaffolding debris from drafting the query) immediately overwritten by the real one - removed it. Deliberately did **not** add an InMemory fake/unit test for this repository (pure aggregation SQL, no business logic to verify without a database - consistent with Category/Warehouse/Supplier/Customer's repositories) - tracked as tech-debt #17 instead. **Not run against real MySQL or viewed in a browser** at authoring time (no Docker daemon available here) - participant should run `docker compose up`, log in as each of the three roles, and confirm the dashboard numbers match what's actually in the seed data before trusting this slice |

| 2026-09-11 | Claude Code | Implemented CSV reports (REPORT-01) - the last WAJIB functional slice from brief §2: date-ranged stock-ledger export and combined PO+SO status export, access mirroring §1.2's download-report row (Admin = both/all, Sales = orders/own only, Warehouse Staff = stock only) | "lanjutkan" (continuing after CSV reports were named as the next item) | Yes - all code kept; `findForReport()`/`findByDateRange()` added to the existing PO/SO/StockLedger repositories (reused by `ReportService`, not a new data path) + matching `InMemory` fake updates so existing PO/SO tests keep passing | Verified the existing `PurchaseOrderServiceTest`/`SalesOrderServiceTest` suites still type-check against the now-larger repository interfaces by updating every `InMemory` fake's method set to match. **No new automated test written** for `ReportService`/`ReportController` (tracked as tech-debt #18 - reporting/query composition, same reasoning as DASH-01's repository) and **not run against real MySQL or clicked through in a browser** at authoring time (no Docker daemon available here) - participant should download both CSVs as each role and confirm row counts/content against known seed data, including two different date ranges per the brief's explicit bukti requirement |

## Notes
- All architecture decisions in `docs/architecture/adr-*.md` must be
  re-explained by the participant unaided during technical defense,
  regardless of how they were drafted.
- Update this table every time an AI tool materially contributes code,
  docs, or design decisions - including "no AI used this session" entries
  if applicable, per the brief's requirement to disclose even non-use.
